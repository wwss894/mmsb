# mmsb1-4脚本的迭代对比

本文档对比了 `mmsb1.py` 至 `mmsb4.py` 四个版本的主要设计思路、功能差异和改进演进。脚本均用于通过 GUI 自动化方式遍历 4 位数字密码，但各自侧重点和成熟度逐渐提升。

---

## 总体概览

| 版本 | 核心定位 | 主要技术手段 | 错误检测 | 可配置性 | 健壮性 |
|------|----------|--------------|----------|----------|--------|
| v1   | 全图像模板匹配 | 屏幕截图 + 模板匹配点击数字、清除、检测错误 | 图像匹配（错误提示） | 低（仅置信度、延迟） | 低（全屏搜索慢，易受分辨率影响） |
| v2   | 固定坐标点击 + 错误检测双模式 | 预置坐标点击数字、清除；错误支持图像或像素检测 | 图像匹配 或 像素颜色阈值 | 中（可切换检测方式） | 中（坐标固定，若窗口位置变化则失效） |
| v3   | 动态坐标计算 + 范围控制 + 中断处理 | 根据“1”键位置和间距自动生成所有数字坐标；支持自定义起始/终止密码 | 同 v2（保留双模式） | 高（密码范围、坐标参数可调） | 中（支持 Ctrl+C 中断和断点续传提示） |
| v4   | 增加休息策略 + 异常加固 | 在 v3 基础上加入定期休息、更完善的异常捕获和日志 | 同 v3，但图像匹配增加灰度选项和文件存在性检查 | 高（增加休息参数） | 较高（带回溯、防风控休息） |

---

## 密码识别各版本详细迭代内容

### 1. mmsb1.py（初版，纯图像模板）

- **数字输入**：使用 `pyautogui.locateCenterOnScreen()` 对每个数字模板（`num_0.png` ~ `num_9.png`）进行全屏匹配，找到后点击中心。
- **清除操作**：同样通过模板匹配点击“清除”按钮。
- **错误检测**：全屏匹配 `error_msg.png` 模板，判断是否出现错误提示。
- **流程**：从 0000 到 9999 顺序尝试，每次先清除，再逐个点击数字，等待固定时间后检测错误，若无错误则认为成功。
- **问题**：
  - 全屏图像匹配极耗性能，遍历 10000 次时间非常长。
  - 模板对屏幕缩放、主题、字体敏感，易匹配失败。
  - 未考虑“确认/提交”按钮的存在，假设自动校验。
  - 清除按钮仅点击一次，可能无法完全清空。
  - 无中途暂停/恢复机制，一旦中断需重头开始。

---

### 2. mmsb2.py（引入固定坐标）

- **关键变化**：放弃数字模板匹配，改用硬编码坐标 `NUM_COORDS` 和 `CLEAR_COORD`，通过 `pyautogui.click(x,y)` 直接点击。
- **错误检测双模式**：
  - 图像匹配（推荐）：仍可选用模板匹配（`USE_IMAGE_DETECTION=True`），但仅针对错误提示。
  - 像素颜色检测：若不用模板，则可固定区域截图并计算平均 RGB，与阈值比较判断是否为错误（如红色背景）。
- **其他**：增加了 Windows DPI 感知设置，避免坐标偏移。
- **遗留问题**：
  - 坐标写死，窗口必须固定位置且无缩放。
  - 清除操作依旧简单，未解决确认按钮问题。
  - 密码范围仍固定为 0000~9999，无法分段。

---

### 3. mmsb3.py（动态坐标 + 自定义范围 + 中断支持）

- **动态生成数字坐标**：用户只需提供数字“1”的中心坐标、水平/垂直间距，自动计算 0~9 的位置（布局为电话键盘样式），大幅减少手动测量。
- **灵活密码范围**：新增 `START_PIN` 和 `END_PIN`，支持从任意四位开始/结束，便于分片任务。
- **中断处理**：捕获 `KeyboardInterrupt`（Ctrl+C），输出当前尝试的密码，并提示将 `START_PIN` 改为下一个值即可继续，实现断点续传。
- **日志优化**：保留双检测模式，并添加 debug 日志输出生成的坐标供核对。
- **不足**：
  - 无休息机制，长时间运行可能触发系统或应用的风控（如限制尝试频率）。
  - 异常捕获较简单，一旦图像检测出错可能直接退出。

---

### 4. mmsb4.py（增加休息策略 + 强化异常处理）

- **休息策略**：新增 `SLEEP_INTERVAL`（每尝试 N 次）和 `SLEEP_DURATION`（休息秒数），模仿人工操作节奏，降低被锁定的风险。
- **图像检测加固**：
  - 检查模板文件是否存在，缺失时返回 True（视为错误）避免误判。
  - 增加 `grayscale=True` 参数，提升匹配稳定性（忽略色彩干扰）。
  - 异常时打印完整回溯（`traceback.format_exc()`），便于调试。
- **其他**：尝试计数器独立，用于触发休息；日志更加详细。
- **仍存在的通用问题**（所有版本均未解决）：
  - 均未考虑“确认/提交”按钮——若界面需要点击确认才能校验，脚本将永远无法正确判断。
  - 清除逻辑仅单击一次，可能不彻底。
  - 错误检测基于一次或两次确认，若系统响应延迟较大，仍可能误判成功。
  - 依赖屏幕分辨率、窗口位置，未做窗口句柄绑定，易受外界干扰。

---

## 改进演进总结

| 功能需求 | v1 | v2 | v3 | v4 |
|----------|----|----|----|----|
| 数字点击方式 | 图像模板匹配 | 固定坐标 | 坐标推算 | 坐标推算 |
| 错误检测方式 | 图像匹配 | 图像/像素双模式 | 双模式 | 双模式（增强） |
| 支持自定义密码范围 | ❌ | ❌ | ✅ | ✅ |
| 支持断点续传/中断提示 | ❌ | ❌ | ✅ | ✅ |
| 休息/防风控机制 | ❌ | ❌ | ❌ | ✅ |
| 异常处理详细程度 | 低 | 中 | 中 | 高 |
| DPI 适配 | ❌ | ✅ | ✅ | ✅ |
| 全屏搜索性能 | 极差 | 较好（坐标） | 较好 | 较好 |

---

## 使用建议

- 若目标应用窗口位置固定且无缩放，**v2** 即可满足基本需求。
- 若需要分片遍历或中途恢复，**v3** 更合适。
- 若担心风控或长时间运行稳定性，**v4** 是最优选择。
- 无论哪个版本，务必先手动测量并验证坐标/模板的准确性，并确认是否需要点击“确认”按钮（如需要，应额外添加该操作）。
